server:
  port: 8060

spring:
  datasource:
    url: jdbc:mysql://localhost:8889/SpringBootDB
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true